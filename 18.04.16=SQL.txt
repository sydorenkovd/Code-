For aruser we have right to selcect and insert, fo optuser only select
GRANT SELECT,INSERT
ON CUSTOMERS
ТО  ARUSER;

GRANT SELECT
ON CUSTOMERS
ТО OPUSER;
---------------------
We can use "all privileges" for short syntax for all rights
If we want to appoint any privilege for all user, just use "TO PUBLIC"

we can also appoint it for precise rows
GRANT UPDATE(COMPANY, CUST_REP)
ON CUSTOMERS
ТО OPUSER;

Наличие предложения  wiтн  GRANT OPTION  означает, что инструкция  GRANT
наряду с привилегиями дает право на предоставление этих привилегий другим
пользователям.
---------------------
Отобрать привилегии с помощью инструкции REVOKE
REVOKE INSERT, UPDATE
ON SALESREPS
FROM OPUSER;
----------------------
Предоставление ролей на основе уже существующих
GRANT OPUSER
ТО FRANCOIS;
---------------------
Повышаем производительность MySQL
- Нужно стараться использовать типы данных минимального размера
- следует хранить значения
даты и времени во встроенных типах данных MySQL, а не в строках.
- При?возможности?избегайте?значений? NULL
- ИСпользовать char для неизменяющихся и коротких строк
- Индексы позволяют находить значение быстрее за счет меньшего количества
 перебора строк, что мы можем проверить используя инструкцию EXPLAIN в кортеже с rows
- Алсо, при построении составного индекса в кортеже key_len мы можем видеть
 количество килобайт затраченых на сами индексы.
- Селективность колонки определяется количеством записей в таблице с одинаковыми
 значениями. Когда записей с одинаковым значением мало — селективность высокая.
 Такие колонки необходимо использовать первыми в составных индексах.
- 
--------------------
Self-join
The SQL SELF JOIN is used to join a table to itself as if the table were two tables, temporarily renaming at least one table in the SQL statement.
For instance:
SELECT a.column_name, b.column_name...
FROM table1 a, table1 b
WHERE a.common_field = b.common_field;














