$this->context->renderPartial('_copyright');
Обращяемся через контекст контроллера, чтобы не нарушать переменную this,
Метод помогает встраивать статические страницы в сайт(копирайты и т.д)
---------------------
abstract factory - когда нужно создать дерево обьктов несколькими разными образами.
dropDownList(
   ArrayHelper::map(Author::find()
->all(), 'id','name'), ['prompt' => 'List of authors'])->hint('Choose the author')
Создает лист из елементов из таблицы автора. Таким образом можно связать две таблицы при создании.